<!--
  SCURM-style NavigateToPose tree adapted for the Gen0 Ackermann vehicle.

  This keeps SCURM's ComputePath -> SmoothPath -> FollowPath structure and
  uses the migrated BackUpTwzFree plugin in non-holonomic mode for recovery.
  GoalUpdater is intentionally omitted because it can block NavigateToPose goal
  acceptance in this Gen0/SCURM stack.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="4" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="0.5">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <Sequence name="ComputeAndSmoothPath">
              <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
              <SmoothPath unsmoothed_path="{path}" smoothed_path="{smoothed_path}"/>
            </Sequence>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{smoothed_path}" controller_id="FollowPath"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </PipelineSequence>
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="AckermannRecoveryActions">
          <BackUp name="FreeSpaceRecoveryShort" backup_dist="0.35" backup_speed="0.10" time_allowance="7"/>
          <BackUp name="FreeSpaceRecoveryLong" backup_dist="0.55" backup_speed="0.10" time_allowance="9"/>
          <Wait name="WaitRecovery" wait_duration="1"/>
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
