# Gen0 清扫车仿真与摄像头画面

本修改基于仓库分支 `gen0_humble-latest-stable-cleanup`。原模型已经包含
`front_camera` 传感器，Gazebo 图像也已经桥接到 ROS 2 话题
`/gen0_model/front_camera`；本补丁新增了实时 OpenCV 画面窗口，并将它接入
主启动文件和一键 3D SLAM 脚本。

## 环境

- Ubuntu 22.04 或带 WSLg 的 WSL2
- ROS 2 Humble
- Gazebo Fortress / Ignition Gazebo 6
- 仓库 README 中列出的 ROS 依赖

额外确认 OpenCV 与 cv_bridge 已安装：

```bash
sudo apt update
sudo apt install -y python3-opencv ros-humble-cv-bridge
```

## 构建

在修改后的仓库根目录执行：

```bash
source /opt/ros/humble/setup.bash
cmake -S gen0_gz_sim_ros2/gz_plugins -B gen0_gz_sim_ros2/gz_plugins/build
cmake --build gen0_gz_sim_ros2/gz_plugins/build -j2
colcon build --symlink-install --packages-up-to gen0_main fast_lio
source install/setup.bash
```

## 启动

完整的清扫 / 3D SLAM 场景：

```bash
./run_gen0_3d_slam.sh
```

启动后会出现 Gazebo、RViz，以及标题为
`Gen0 Sweeper - Front Camera` 的摄像头窗口。窗口左上角显示 ROS 图像话题、
原始分辨率和实时 FPS。按 `q` 或 `Esc` 关闭摄像头窗口。

只启动仿真场景和摄像头窗口：

```bash
ros2 launch gen0_main spawn.launch.py \
  world:=my_map \
  gazebo_gui:=true \
  camera_view:=true
```

可选参数：

```bash
# 一键脚本中禁用摄像头窗口
GEN0_CAMERA_VIEW=false ./run_gen0_3d_slam.sh

# 主启动文件中切换图像话题或限制窗口尺寸
ros2 launch gen0_main spawn.launch.py \
  camera_topic:=/gen0_model/front_camera \
  camera_view_width:=960 \
  camera_view_height:=720
```

## 验证

```bash
ros2 topic hz /gen0_model/front_camera
ros2 topic info /gen0_model/front_camera
```

如果窗口一直显示等待图像，先检查 Gazebo 是否在使用带渲染能力的 OGRE
后端，并确认 WSLg/X11 图形转发可用。无图形环境时传入
`camera_view:=false`。

`Unknow-camera-overlay.zip` 中保留了相对于仓库根目录的文件结构，可直接解压
覆盖到同一分支的干净副本，然后重新执行 `colcon build`。
